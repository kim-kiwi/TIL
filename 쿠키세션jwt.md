## 쿠키

서버가 생성하여 클라이언트에 저장하는 값

키-값 형식으로 되어있다.

http-only 옵션을 켜면 브라우저가 JS로부터 해당 쿠키를 보호해준다.

## 세션

서버가 생성하여 서버에 저장하는 값

클라이언트는 세션 ID를 가지고 있고 그걸 서버에 보내면 서버가 세션 DB에서 조회해서

해당 세션 ID에 해당하는 값들을 얻을 수 있다. 서버가 넘겨주는게 아니면 클라이언트는 절대

세션 값을 알 수 없다.

## Jwt

인증에 필요한 데이터를 토큰 형태로 만드는 것.

누구나 내용을 볼 수 있고 변경할 수 있지만, 서명을 모르면

위/변조 검증에서 막힌다.

누구나 볼 수 있는건 변함 없기에 토큰에 중요한 내용을 담으면 안된다.

