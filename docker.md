# Docker

코드를 실행할때 주변환경(라이브러리, 설치된 프로그램, 환경변수)등을 일관되게 유지할 수 있도록 컨테이너를 만드는 프로그램

## Container

외부에서는 평범한 프로세스로 보인다.

주변 프로세스를 알 수 없도록 격리당한다.

또한 정해진 디렉토리를 무조건 상위(`/`) 주소로 강제당하기 때문에 지정되지 않은 파일들을 수정할 수 없다.

메모리 사용량을 제한할 수 있다.

외부와 격리되었기 때문에 외부 요인 없이 항상 정해진 환경에서 프로그램이 실행된다.

그러므로 돌발상황이 적어진다.

## Image

컨테이너를 만드는 틀이다.

앱 실행에 필요한 파일과 설정이 들어있다.

이미지 생성용 DSL을 통해 만든다.

## Compose

컨테이너 여러개 만들때 쓴다.

yaml 형식으로 작성된다.

컨테이너 여러개 관리하기 쉽다.

## 멀티 컨테이너

컨테이너별로 책임이 하나라서 관리가 쉽다.

여러 프로그램중 특정 프로그램 하나만 변경하기도 쉽다.

프로그램마다 환경이 섞이지 않는다. -> DB서버는 A프로그램의 버전 1, 백엔드 서버는 A프로그램의 버전 2를 사용할 수도 있다는 뜻.
