# 트랜잭션

DB 상태 변경을 위한 작업의 최소 단위

## 격리성

한 트랙잭션이 다른 트랜잭션에게 방해받지 않는 정도

 - Uncommited Read - 한 트랜잭션이 데이터 변경 후 커밋하지 않아도 다른 트랜잭션이 데이터를 읽을 수 있음. Dirty Read 발생
 - Commited Read - 위와 반대로 커밋해야 읽을 수 있음. Non Repeatable Read 발생
 - Repeatable Read - Commited Read의 Non Repeatable Read를 해결하기 위해 현재 트랜잭션보다 먼저 생성된 트랜잭션이 커밋한 데이터만 읽는다.
 - Serialize - 트랜잭션을 무조건 순차적으로 실행한다. 관리가 훨씬 쉽지만 성능이 구리다.

 - Dirty Read - A가 데이터 바꿈, B가 읽음, A가 Undo함 -> B의 데이터는 유효하지 않음.
 - Non Repeatable Read - 동일 트랜잭션을 두 번 실행하면 결과가 바뀜.

# 데드락

예시:
```
A 프로세스가 R2를 가진채로 R1 리소스를 요청
B 프로세스가 R1을 가진채로 R2 리소스를 요청
무한 대기 발생
```

## 발생

데드락 조건
 - 상호 배제 - 자원이 한 번에 한 프로세스만 사용 가능
 - 점유 대기 - 자원을 가진채로 다른 자원을 기다림
 - 비선점 - 남의 자원을 뺏을 수 없음
 - 순환 대기 - 서로가 서로의 자원을 기다림

4개가 모두 성립해야함.
즉, 데드락을 해결하려면 네 개의 조건중 하나 이상을 제거하면 됨.

## 해결
 - 예뱡 - 데드락 조건중 하나를 원천적으로 제거하여 데드락이 절대 불가능하게 함
 - 회피 - 어떤 자원 요청이 교착 상태를 일으킬 가능성이 있는지 검사하여 할당함
 - 탐지 및 복구 - 데드락 발생을 허용하고, 발생 시에 조치함
